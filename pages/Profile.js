import BaseLayout from "@/components/BaseLayout";
import Hero from "@/components/Hero";

const Profile = () => {
  return (
    <BaseLayout>
        <Hero/>
    </BaseLayout>
);
};

export default Profile;
